export const metadata = {
    title: "HustTracker - Course Details",
    description: "View and manage course details on HustTracker.",
};

export default function CourseDetailLayout({ children }) {
    return <>{children}</>;
}
