export const metadata = {
    title: "HustTracker - Courses",
    description: "View and manage courses on HustTracker.",
};

export default function CourseLayout({ children }) {
    return <>{children}</>;
}
